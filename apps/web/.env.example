# Local dev defaults are fine if you're running the FastAPI backend on
# the default docker-compose port (8000). Copy this to .env.local to
# override.

# Backend base URL. NO trailing slash.
NEXT_PUBLIC_API_URL=http://localhost:8000
